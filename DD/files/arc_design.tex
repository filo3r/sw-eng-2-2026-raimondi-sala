\subsection{Overview}
The BBP system is based on a client-server architecture intended to guarantee a separation of responsibilities. The overall structure follows the three-tier architecture pattern, which divides the system into the following logical layers: presentation, application, and data.

In general, the system works as follows: the user interacts with a responsive web interface. This interface sends requests via secure HTTPS protocol to a central server then the server verifies the user's identity, processes requests by applying business rules, and coordinates interaction with third-party services for geographic and meteorological features. Finally, persistent data is saved and retrieved by a database management system optimized for spatial data.

The components that make up these levels are described in detail below:

\subsubsection{Presentation Layer}
The presentation layer consists of a Multi Page Application developed with the Vue.js framework. This component resides on the user's device and it provides the user a dynamic user experience. Its main functions include managing the user interface by rendering pages and forms for registration and login, as well as geographical visualization through the integration of interactive maps to show routes and obstacles.

\subsubsection{Application Layer}
The backend is a server application based on Java Spring Boot. This layer exposes a set of RESTful APIs that serve as a single point of entry for the frontend. One of the most critical part of the project is the security of the user's personal data, therefore it is in the application layer where most of the security resides, access to private accounts is implemented through credential verification and JSON Web Tokens validation for each request. The server processes users' data by implementing domain logic, such as calculating travel statistics and validating the quality of bike routes. In the end it also handles the API requests to external services and transforms raw data into useful information for the user.

\subsubsection{Data Layer}
Persistence is handled by the relational database PostgreSQL. A key feature of this layer is the use of the spatial extension PostGIS. The database stores both simple type such as integers and strings but also complex geometries, this allows the system to perform efficient spatial queries directly at the database level.

\subsubsection{External Services}
The BBP system uses external services for functions that are too complex to be made for this project. Specifically, Mapbox is used for both the geocoding service, that means converting addresses into coordinates, and the routing engine needed to calculate the best route. BBP also uses Open-Meteo to enrich travel data with historical weather information based on date and location.

\subsubsection{Architectural Diagrams}
To visualize the structure described above, the architectural diagrams of the system are shown below. Figure \ref{fig:high_level_arch} illustrates the high-level architecture, showing the communication flows between the client, server, database, and external services.

\begin{figure}[h!]
    \centering
        \includegraphics[height=0.5\textheight, keepaspectratio]{BBP/images/high-level-arch.png}
    \caption{High level architecture of the system}
    \label{fig:high_level_arch}
\end{figure}

\subsection{Component View}
The BBP system consists of several software components that work together to provide the functionality described in the RASD. The architecture, as stated previously, is based on the client-server paradigm, with a distinct separation between business logic, also known as backend, the user interface, also known as frontend, and the external services.

\subsubsection{Backend Components}
The backend is composed of monolithic services managed by the Spring Boot framework, each microservice is responsible for a distinct functional domain and exposed via a REST API.

\begin{itemize}
    \item \textbf{Authentication Service}: This component manages the security and accesses on the platform, it's implemented with UserAuthService and JwtService, and it handles the processes of registration, login, and the geenration of JSON Web Token.
    
    
    \item \textbf{User Management Service}: This component manages the user profile. It consents users to modify their credentials and to manage their personal data.
    
    \item \textbf{Trip Service}: This component manages the trip recording and history. It saves all necessary data collected during biking sessions and elaborates statistics. It also works on the data enrichment as at the end of a trip, the service makes a request to the external Weather Data Provider to associate historical weather conditions with the time and location of the completed activity.
    
    \item \textbf{Sensor Analysis Service}: This component is dedicated of the elaboration of the data coming from the sensors, therefore it's the component that manages the automatic detection of obstacles. It receives raw data from sensors such as gyroscope and accelerometer and uses known algorithms to identify patterns that are indicative of the presence of possible potholes or other obstacles in the end it sends the results to the Obstacle Service.
    
    \item \textbf{Obstacle Management Service}: This component is responsible for the handling of the obstacles, it acquires the obstacles reports, both manually inserted and automatically detected, it then validates that the obstacles are within a buffer distance from the bike path, and if so, it saves them in the database.
    
    \item \textbf{Bike Path Service}: This component manages the bike paths and it handles the creation of routes, moreover it implements the logic for elaborating the Quality Score, evaluated based of the conditions of the road surface and the total amount of obstacles on that route.
    
    \item \textbf{Bike Path Service}: This component oversees the management of bike paths, it manages the generation of routes and executes the logic to compute the Quality Score, calculating it from the state of the road surface and the total number of obstacles associated with the path.
    
    \item \textbf{Bike Path Finder Service}: This component elaborates the spatial queries for locating the rotues within a geographical area and it applies the filter specified by the user.
    
    \item \textbf{Mapbox Integration Service}: This component acts as a wrapper for MapBoxService as it decouples the backend from the external service provider, it handles the geocoding and the provision of the tokens required for client side rendering.
    
    \item \textbf{DBMS}: This component acts as central storage for all system data, it uses a relational database with a spatial extension to handle geolocated entities such as obstacles.
\end{itemize}

\subsubsection{Frontend Components}
\begin{itemize}
    \item \textbf{Web \& Mobile Application}: This component represents the main point of interaction for users. It is a multi page application that communicates with the backend via REST API. It manages the display of dashboards, interactive maps, and user profiles.
    
    \item \textbf{Sensor Data Collector}: This component is active on mobile devices during tracking. It interfaces with the device hardware to collect real-time data from GPS, accelerometer, and gyroscope. It performs light pre-processing of the data to optimize transmission to the back-end Sensor Analysis Service.
\end{itemize}

\subsubsection{External Services}
These components are external to the BBP system but essential for its operation.

\begin{itemize}
    \item \textbf{Map Provider (Mapbox)}: This component provides the mapping functionalities, such as nice map tiles and accurate routing data.
    
    \item \textbf{Weather Data Provider (OpenMeteo)}: This component provides historical and current weather data.
\end{itemize}

\begin{figure}[h!]
    \centering
        \includegraphics[height=0.8\textheight, keepaspectratio]{BBP/images/component-view.png}
    \caption{Component view}
    \label{fig:component-view}
\end{figure}
\clearpage

\subsection{Deployment View}
BBP system adopts a distributed architecture based on the three-tier model, its software components resides on physically separated nodes and interact with each other via standard network protocols.

\subsubsection{Nodes Deployment}

\begin{itemize}
    \item \textbf{Client Node}: This node identifies the user's device, the execution enviroment requires a modern web browser  that supports HTML5 and Javascript in the case the user uses the web interface, otherwise it needs a phone with the most recent android or iOS version. Moreover for the automatic detection to work, the client node will need a gyroscope, and an accelerometer.

    \item \textbf{Web \& Application Server}: This node constitutes the core of the infrastructure and is hosted on the cloud, it uses a reverse proxy to handle requests on port 443, that component distributes the static files of the frontend and acts as a gateway by forwarding the API calls to the backend. The execution environment launches the Java artifact of the backend, where the Spring Boot application incorporates a Tomcat server listening on port 8080.

    \item \textbf{Database Server}: This node runs on a dedicated server for data persistence, the DBMS is PostgreSQL with the PostGIS extension enabled to provide native support for geospatial data.

    \item \textbf{External Service Providers}: These are third-party systems necessary for the operation of BBP, in particular, Mapbox provides APIs for vector maps, routing, and geocoding, while OpenMeteo provides APIs for retrieving historical weather data.
\end{itemize}

\begin{figure}[h!]
    \centering
        \includegraphics[height=0.8\textheight, keepaspectratio]{BBP/images/deployment-view.png}
    \caption{Deployment view}
    \label{fig:deployment-view}
\end{figure}
\clearpage

\subsection{Runtime view}
This section describes the dynamic interaction between system components that realize the use cases.

\textbf{UC1: Manual Trip Insertion}

The user manually insert a bike path specifing the route he took and at what time, the system elaborates the correct route and, if available, it enriches the data with weather data before saving it.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc1.png}
    \caption{Sequence Diagram - UC1 Manual Trip Insertion}
    \label{fig:seq_uc1}
\end{figure}

After the submission of data by the user, TripService invokes the MapboxService to get the exact geometry of the trip and the travelled distance, then it make an API request to the OpenMeteo service to get the historical weather conditions at the time of the trip. In the end it saves the entity on the database and sends the user a confirmation.

\clearpage

\textbf{UC2: Creation of a Public Bike Path}

The user creates a bike path defining the points on a map, the system calculate the geometry, validates that the insertion of obstacles are withing the range of the selected path and elaborates the Quality Score.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc2.png}
    \caption{Sequence Diagram - UC2 Creation of a Public Bike Path}
    \label{fig:seq_uc2}
\end{figure}

The system receive the waypoints and uses the external service to determine the correct trip geometry. A spatial validation is performed to ensure that obstacles are within a limit distance from the route, at the end the system then calculates the Quality Score and saves the track with the visibility settings chosen by the user.

\clearpage

\textbf{UC3: Search of Bike Paths}

The user searches for bike path by entering the origin and destination. The system returns the path found sorted by score.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc3.png}
    \caption{Sequence Diagram - UC3 Search of Bike Paths}
    \label{fig:seq_uc3}
\end{figure}

The BikePathService performs a spatial query to find routes in the requested area that originate from the starting point and arrive to the destination point, sorting them by descending score. When the user selects a specific route, the system retrieves and displays all available details.

\clearpage

\textbf{UC4: Obstacle Reporting on Bike Path}

A user reports an obstacle on an existing track. The system validates the location and updates the track by recalculating its score.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc4.png}
    \caption{Sequence Diagram - UC4 Obstacle Reporting}
    \label{fig:seq_uc4}
\end{figure}

After receiving the report, the system verifies the geographical consistency of the obstacle with respect to the geometry of the track, if valid, the obstacle is saved, the track score is recalculated, and the update visible to all users.

\clearpage

\textbf{UC5: Automatic Tracking}

The mobile app records the journey and detects anomalies using sensors. In the end, the user confirms the anomalies and the data is uploaded to the server.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc5.png}
    \caption{Sequence Diagram - UC5 Automatic Tracking}
    \label{fig:seq_uc5}
\end{figure}

While the user is cycling, the app records the route and identifies potential anomalies locally with a low degree of confidence. When the user stops, the app detects the stop, finalizes the route, and sends preliminary anomaly data to the backend.
The server responds by requesting confirmation: the app then displays a prompt to the user to validate or modify the detected obstacles. In the end the app sends the complete payload to the server, which saves the entire trip in the database via the TripService.

\clearpage

\textbf{UC6: User Registration}

User registration.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc6.png}
    \caption{Sequence Diagram - UC6 User Registration}
    \label{fig:seq_uc6}
\end{figure}

The system validates the uniqueness of the email and the correctnes of the inserted data. It creates the new account by encrypting the password, generates a session token, and redirects the user to the dashboard.

\clearpage

\textbf{UC7: User Login}

User login.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc7.png}
    \caption{Sequence Diagram - UC7 User Login}
    \label{fig:seq_uc7}
\end{figure}

The system verifies the credentials provided, if those are correct, it generates a JWT token for the user.

\clearpage

\textbf{UC8: Management of Created Bike Paths}

The user manages the routes created, being able to modify their visibility or delete them.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc8.png}
    \caption{Sequence Diagram - UC8 Management of Created Bike Paths}
    \label{fig:seq_uc8}
\end{figure}

The system lists the user's paths, depending on the action chosen, the system updates the path metadata or permanently deletes the path.

\clearpage

\textbf{UC9: Trip History Visualization and Management}

View travel history with statistics and weather data.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc9.png}
    \caption{Sequence Diagram - UC9 Trip History Visualization}
    \label{fig:seq_uc9}
\end{figure}

The system retrieves the user's trips, displaying statistics and associated weather conditions. The user can filter the list or request the deletion of a specific trip, which the system removes from the database.

\clearpage

\textbf{UC10: User Profile Update}

The user modifies their personal data.

\begin{figure}[h!]
    \centering
        \includegraphics[width=\linewidth]{BBP/images/seq_uc10.png}
    \caption{Sequence Diagram - UC10 User Profile Update}
    \label{fig:seq_uc10}
\end{figure}


The system validates the new data sent, if valid, it updates the user profile and confirms the success of the operation.

\clearpage

\subsection{Component Interfaces}
This section shows the interfaces exposed by all the components of the system.

\subsubsection{REST API Interface (Frontend to Backend)}
This interface is exposed by Spring Boot Controllers and consumed by the Client.
\begin{itemize}
    \item \texttt{POST /auth/register(UserRegisterRequest)}
    \item \texttt{POST /auth/login(UserLoginRequest)}
    \item \texttt{GET /users/me()}
    \item \texttt{PATCH /users/me(UserUpdateRequest)}
    \item \texttt{DELETE /users/me()}
    \item \texttt{GET /trips(page, size, sort)}
    \item \texttt{POST /trips/search(TripSearchRequest)}
    \item \texttt{POST /trips/manual(TripManualRecordRequest)}
    \item \texttt{DELETE /trips/\{id\}()}
    \item \texttt{GET /bike-paths(page, size, sort)}
    \item \texttt{POST /bike-paths/search(BikePathSearchRequest)}
    \item \texttt{POST /bike-paths/manual(BikePathManualCreateRequest)}
    \item \texttt{PATCH /bike-paths/\{id\}(BikePathUpdateRequest)}
    \item \texttt{DELETE /bike-paths/\{id\}()}
    \item \texttt{POST /finder/search(BikePathFinderRequest)}
\end{itemize}

\subsubsection{Logic Interfaces}
These interfaces represent the internal business logic invoked by Controllers.

\textbf{UserAuthService Interface}
\begin{itemize}
    \item \texttt{UserAuthResponse register(UserRegisterRequest request)}
    \item \texttt{UserAuthResponse login(UserLoginRequest request)}
    \item \texttt{User getAuthenticatedUser()}
\end{itemize}

\textbf{UserService Interface}
\begin{itemize}
    \item \texttt{User updateCurrentUser(UserUpdateRequest request)}
    \item \texttt{void deleteCurrentUser()}
\end{itemize}

\textbf{TripService Interface}
\begin{itemize}
    \item \texttt{Trip recordTripManually(TripManualRecordRequest request)}
    \item \texttt{Page<Trip> getUserTrips(int page, int size, String sort, String dir)}
    \item \texttt{Page<Trip> searchTrips(TripSearchRequest request, ...)}
    \item \texttt{void deleteTrip(Long id)}
\end{itemize}

\textbf{BikePathService Interface}
\begin{itemize}
    \item \texttt{BikePath createBikePathManually(BikePathManualCreateRequest req)}
    \item \texttt{BikePath updateBikePath(Long id, BikePathUpdateRequest req)}
    \item \texttt{Page<BikePath> getUserBikePaths(int page, int size, ...)}
    \item \texttt{Page<BikePath> searchBikePaths(BikePathSearchRequest req, ...)}
    \item \texttt{void deleteBikePath(Long id)}
\end{itemize}

\textbf{ObstacleService Interface}
\begin{itemize}
    \item \texttt{void validateObstaclesInPath(List<Coordinate> path, List<Obstacle> obs)}
    \item \texttt{Obstacle createObstacle(ObstacleCreateRequest req)}
    \item \texttt{List<Obstacle> getObstaclesByPathId(Long pathId)}
\end{itemize}

\textbf{SensorAnalysisService Interface (Automation)}
\begin{itemize}
    \item \texttt{void analyzeSensorData(List<SensorData> data, Trip currentTrip)}
    \item \texttt{List<Anomaly> detectAnomalies(List<AccelerometerData> accData)}
\end{itemize}

\subsubsection{External Services Interfaces}
These interfaces are used by internal adapters to communicate with third-party systems.


\textbf{Mapbox Interface}
\begin{itemize}
    \item \texttt{RouteResult getRoute(Coordinate origin, Coordinate dest)}
    \item \texttt{MatchResult matchRoute(List<Coordinate> rawPoints)}
    \item \texttt{GeocodeResult geocodeAddress(String address)}
\end{itemize}

\textbf{OpenMeteo Interface}
\begin{itemize}
    \item \texttt{WeatherResult getHistoricalWeather(double lat, double lon, LocalDateTime time)}
\end{itemize}

\subsubsection{DBMS Interfaces (Repository Layer)}
These interfaces correspond to persistence operations towards the PostgreSQL database.

\textbf{User Repository}
\begin{itemize}
    \item \texttt{query\_findByEmail(String email)}
    \item \texttt{query\_existsByEmail(String email)}
    \item \texttt{store\_save(User user)}
    \item \texttt{delete\_deleteById(Long id)}
\end{itemize}

\textbf{Trip Repository}
\begin{itemize}
    \item \texttt{query\_findAllByUserId(Long userId, Pageable page)}
    \item \texttt{query\_findAll(Specification<Trip> spec, Pageable page)}
    \item \texttt{store\_save(Trip trip)}
    \item \texttt{delete\_deleteById(Long id)}
\end{itemize}

\textbf{BikePath Repository}
\begin{itemize}
    \item \texttt{query\_findWithinBbox(Polygon bbox)}
    \item \texttt{query\_findAllByOwnerId(Long userId, Pageable page)}
    \item \texttt{query\_findAll(Specification<BikePath> spec, Pageable page)}
    \item \texttt{store\_save(BikePath path)}
    \item \texttt{delete\_deleteById(Long id)}
\end{itemize}

\textbf{Obstacle Repository}
\begin{itemize}
    \item \texttt{query\_findByBikePathId(Long pathId)}
    \item \texttt{store\_save(Obstacle obstacle)}
\end{itemize}

\textbf{MeteorologicalData Repository}
\begin{itemize}
    \item \texttt{store\_save(MeteorologicalData data)}
    \item \texttt{query\_findByTripId(Long tripId)}
\end{itemize}


\subsection{Selected Architectural Styles and Patterns}
The architecture of the BBP system integrates various notorious patterns of software engineering. The combination of these solutions has been designed to ensure a high degree of modularity and to simplify code maintenance.

\subsubsection{Client-Server Architecture (RESTful)}
The system is based on a client-server architecture that completely decouples the presentation layer from the business logic and data. The frontend, built in Vue.js, and the backend, based on Spring Boot, operate as separate entities that communicate exclusively through RESTful API interfaces. The server exposes stateless HTTP endpoints that return data in standard JSON format, consumed by the client via asynchronous libraries.

\subsubsection{Layered Architecture (N-Tier)}
Internally, the backend is structured according to a layered architecture pattern, imposing a strict segregation of responsibilities. The flow of a request passes through three distinct layers:
\begin{enumerate}
    \item \textbf{Controller Layer}: Manages the HTTP interface, validating inputs and serializing responses.
    \item \textbf{Service Layer}: Encapsulates the core logic of the application.
    \item \textbf{Repository Layer}: Abstracts access to persistent data.
\end{enumerate}
This layering reduces coupling between components while facilitates the writing of isolated unit tests, allowing business logic to be tested independently of the other parts.

\subsubsection{Dependency Injection \& Inversion of Control}
The Spring Boot framework is used to implement the principle of Inversion of Control through Dependency Injection. Instead of manually instantiating dependencies, components delegate this responsibility to the Spring container, which automatically injects the necessary instances when the application starts. This approach, managed through annotations such as @Autowired or @RequiredArgsConstructor, makes the code more testable, allowing for the easy injection of mock objects during testing.

\subsubsection{Data Transfer Object Pattern}
To decouple the internal domain model from the contract exposed externally via APIs, the system adopts the Data Transfer Object pattern. Data do not travel across the network as database entities, but are encapsulated in objects. The use of specific mappers for Entity-DTO conversion allows sensitive attributes to be hidden, data from different sources to be aggregated, and public APIs to remain stable even when changes are made to the database schema.

\subsubsection{Adapter Pattern}
Integration with third-party services, such as Mapbox for mapping and Open-Meteo for climate data, is managed through the Adapter Pattern. Specific classes such as MapboxService act as adapters, exposing high-level, semantically rich methods to the domain while hiding the complexity of raw HTTP calls and parsing external responses. This isolation protects the business logic from any changes in external providers' APIs.

\subsubsection{Repository Pattern}
Data access is abstracted via the Repository Pattern, implemented with Spring Data JPA. Dedicated interfaces provide a collection-like view of domain objects, hiding the details of the underlying SQL queries. This pattern in addition of standardizing the creation, reading, updating and deletion operations, it also allows for the declarative definition of complex queries, including PostGIS spatial operations.

\subsection{Other Design Decisions}

In addition to structural architectural patterns, specific technical decisions were made to meet the security and data management requirements.

\subsubsection{Spatial data handling: PostGIS \& JTS}
Given the geospatial nature of the project, the system leaves the representation of coordinates as simple numerical pairs in favor of native geometric types. The adoption of PostgreSQL with the PostGIS extension on the database and the Java Topology Suite library in the backend allows the computational load of spatial operations to be delegated to the DBMS. Complex queries, such as searching for paths within a bounding box or calculating intersections, are performed natively and efficiently directly at the database level.

\subsubsection{Stateless Authentication: JSON Web Token}
To maximize scalability, the system uses a stateless authentication mechanism based on JSON Web Token standards, deleting the need to manage server-side sessions and session cookies. Each authenticated request carries a signed token that identifies the user, allowing the backend to scale horizontally without having to replicate the session state across nodes. Furthermore, this approach facilitates cross-platform integration, allowing web and mobile clients to handle authentication in a uniform manner.