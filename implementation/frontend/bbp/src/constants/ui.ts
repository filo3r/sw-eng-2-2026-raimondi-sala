/**
 * UI timing constants for loading states and animations.
 * Improves perceived performance by preventing loading spinner flashes.
 */

/** Delay in milliseconds before showing loading spinner (prevents flash on fast network requests) */
export const SPINNER_DELAY_MS = 500